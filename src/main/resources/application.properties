spring.config.import=optional:file:.env[.properties]

spring.application.name=SecureFintechBank

server.port =${PORT:8090}

#spring.datasource.url=jdbc:mysql://centerbeam.proxy.rlwy.net:27765/secure_fintech_bank?useSSL=false&allowPublicKeyRetrieval=true
#spring.datasource.username=root
#spring.datasource.password=DKxYzBiPasblBeSvNifjArXZiFlKjrjB
#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
#server.port=${PORT:8090}

#spring.datasource.url=${LOCAL_DB_URL}
#spring.datasource.username=${LOCAL_DB_USERNAME}
#spring.datasource.password=${LOCAL_DB_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

spring.datasource.url=${PROD_DB_URL}
spring.datasource.username=${PROD_DB_USERNAME}
spring.datasource.password=${PROD_DB_PASSWORD}

#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JWT Configuration
#jwt.secret.string=${JWT_SECRET}
#jwt.expiration.time=${JWT_EXPIRATION_TIME}

jwt.secret.string=secureBankJwtSecretKey2026secureBankJwtSecretKey2026secureBankJwtSecretKey2026
jwt.expiration.time=2592000000


#Mail Configuration
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASS}
spring.mail.host=smtp.gmail.com
spring.mail.port=587

password.reset.link=http://localhost:3000/reset-password?code=

#Enable TLS SMTP MAIL
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

#AWS Credentials
#aws.accessKeyId=YOUR_AWS_ACCESS_KEY
#aws.secretAccessKey=YOUR_AWS_SECRET_KEY
#aws.region=us-east-1
#aws.s3.bucket=your-bucket-name